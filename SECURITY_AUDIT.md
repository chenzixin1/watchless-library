# 代码与安全审查记录

审查日期：2026-09-24。审查基线：`56434b9`，本文件随本次修复一起提交。

## 范围与结论

检查了站点与模板 JavaScript、入库与安装脚本、本地预览服务、内置 Watchless 的获取/转录/选帧/导出路径，以及 video-use 辅助程序的命令执行和网络调用。重点是外部输入如何进入路径、网页、缓存、文件替换和云端请求。

发现并修复下列具体问题。此前报告中的“94/100”“未发现跨目录破坏行为”等结论缺乏充分验证，本记录替代该报告，不给出安全分数或零漏洞承诺。

## 已修复问题

| 级别 | 问题与触发条件 | 修复 |
| --- | --- | --- |
| P1 | 入库 `--id` 未执行文档宣称的字符校验，`../` 或绝对路径可使媒体写入、frames 删除越出站点；目标符号链接也可能改变写入位置 | 校验课程 ID；写入前核对路径边界，拒绝符号链接目标 |
| P1 | 本地服务沿用标准目录服务，会跟随站点内指向外部的符号链接，暴露隐藏的源文件元数据，且不检查 Host | 限制站点根目录、隐藏路径和目录列表；只接受 loopback/localhost Host，降低 DNS rebinding 风险 |
| P1 | 独立 HTML 导出默认允许笔记中的原始 HTML，脚本内容可能进入生成页面 | Pandoc 禁用 raw HTML/raw attributes；可信样式独立传入，并添加限制脚本和外部请求的 CSP |
| P2 | 来源链接直接写入 `href`，没有约束 URL 协议 | 入库和页面渲染均只接受 HTTP/HTTPS 来源链接 |
| P2 | `parse_cues` 要求每行含说话人冒号；Whisper 的无说话人句子会全部丢失，正文冒号也可能被误认为说话人 | 支持无说话人时间轴，仅剥离已知标签，保留正文冒号并按起始时间排序 |
| P2 | 火山引擎/Whisper 音频缓存只按视频文件名命名，同名或更新后的视频可能识别错误音频 | 按视频内容 SHA-256 缓存，成功后替换；缓存迁出源码目录 |
| P2 | prepare 仅检查转录文件是否存在，切换 provider、语言或视频后仍复用旧结果 | 记录源文件指纹、provider、语言和来源字幕指纹；仅匹配时复用，失败时保留原转录 |
| P2 | 本地同名视频共享处理目录；中文标题清洗后可能共享默认课程 ID | 处理目录加入来源路径摘要；入库记录来源标识并复用已有记录，默认 ID 加摘要，避免同名覆盖 |
| P2 | 缓存的本地视频更新后仍沿用旧采集时长等元数据 | 采集缓存复用前核对源文件指纹 |
| P2 | 目录解析失败时静默重建，会丢失已有课程入口 | 在写入任何媒体前读取并校验目录，异常时停止；数据文件使用临时文件替换 |
| P2 | 视频转码前删除旧文件，失败后原可播放视频消失；源目标相同也会误删源视频 | 视频先写临时文件，成功后替换；禁止源目标同路径。封面与关键帧也采用准备后替换 |
| P2 | 已保存语言版本可能在章节改变后被继续套用；缺少 scenes 的翻译对象会使前端报错 | 入库前校验语言数据及场景对应关系，章节变化要求重新生成语言数据；前端增加数组检查 |
| P2 | YouTube 域名采用字符串后缀匹配，`fake-youtube.com` 也会被接受 | 校验 HTTP/HTTPS 与精确域名/子域名边界 |
| P3 | Whisper 句级时间戳被标记为词级；缺少 ffprobe 时自检直接崩溃；阅读模式切换复用旧高亮索引 | 按实际时间戳粒度记录元数据；工具缺失正常报告；模式切换重置高亮索引 |

修复同步至 `site/tools/ingest.py` 与 `assets/site-template/assets/site.js`，现有课程媒体和内容未重新生成。

## 验证

- `python3 -m pytest tests dependencies/video-use/tests -q`：**36 passed，18 subtests passed**。
- 覆盖：越界 ID、符号链接、隐藏文件、Host 检查、GET/HEAD/Range 字节结果、无说话人字幕、正文冒号、失败转码保护、损坏目录保护、双语数据校验、同名课程累积与重复入库、音频缓存隔离、转录缓存失效、YouTube 域名边界、实际 Pandoc HTML 渲染。
- 27 个 Python 文件语法解析通过；站点 JavaScript 语法检查通过。
- Skill 元数据检查通过；模板/站点脚本及两份入库脚本一致。
- 内置浏览器：重启更新后的本地服务，实际课程加载、中文/英文切换和 07:52 跳播通过。
- 对 Git 跟踪的小于 2 MB 文件进行常见私钥/GitHub/AWS/OpenAI 密钥特征检查，未命中；这是有限模式检查，不代表能识别所有凭证。

## 边界与后续验证

- 云端转录、YouTube 下载和完整模型推理没有在本次审查中重新执行。ASR 参数、缓存等测试使用模拟服务，不产生云端费用。
- 未做第三方 Python 包的完整依赖解析与 CVE 扫描，也未对 ffmpeg、浏览器和模型权重做独立安全审计。
- 本地服务只用于本机预览，没有账号或鉴权；不应直接作为公网服务部署。
- 入库为本地文件工作流，应串行操作同一站点。单文件替换保护不等同于整个站点具备数据库事务。
- 已有笔记与字幕的事实准确性、翻译质量、估算字幕时间不属于本次代码测试证明的范围。
